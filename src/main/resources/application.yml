server:
  port: 8081

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: root
    password: security
    url: jdbc:postgresql://localhost/jpa-ecommers
    hikari:
      maximum-pool-size: 10
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

